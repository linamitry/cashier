import { ReceiptProductUpdate } from './receipt-product-update';

describe('ReceiptProductUpdate', () => {
  it('should create an instance', () => {
    expect(new ReceiptProductUpdate()).toBeTruthy();
  });
});
